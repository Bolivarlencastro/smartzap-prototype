export const API_ERROR_KEYS: Record<string, string> = {
  'The user has an enrollment in progress. Users can re-enroll in the same course when has an enrollment completed or drop outed.':
    'API.ERROR.ENROLLMENT_IN_PROGRESS',
};
