{
  "extends": ["../../.stylelintrc.json"],
  "ignoreFiles": ["!**/*", "artifacts/**/*.css", "src/app/layout/**/*.scss", "libs/fuse/**/*.scss"],
  "overrides": [
    {
      "files": ["**/*.scss"],
      "rules": {}
    }
  ]
}
