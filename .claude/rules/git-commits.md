# Commit Convention

Conventional commits are enforced via commitlint:

```
<type>(clickup-card-code): <description>

# Examples:
feat(DEV-12345): add new enrollment filter
fix(DEV-12345): correct quiz submission logic
test(DEV-12345): update mission e2e tests
```
