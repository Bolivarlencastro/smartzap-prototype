export interface Job {
  id: string;
  name: string;
  created_date?: string;
  updated_date?: string;
  workspace?: string;
}

export interface JobFunction {
  id: string;
  name: string;
  created_date?: string;
  updated_date?: string;
  workspace?: string;
}
