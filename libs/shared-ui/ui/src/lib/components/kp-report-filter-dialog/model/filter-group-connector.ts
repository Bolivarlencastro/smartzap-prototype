export enum FilterGroupConnector {
  WHERE = 'WHERE',
  AND = 'AND',
  OR = 'OR',
}

export const FilterGroupConnectors = [FilterGroupConnector.WHERE, FilterGroupConnector.AND, FilterGroupConnector.OR];
