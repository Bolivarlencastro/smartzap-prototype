import { KpSidenavFooterDirective, KpSidenavContentDirective } from './kp-sidenav-directives';

describe('KpSidenavFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new KpSidenavFooterDirective();
    expect(directive).toBeTruthy();
  });
});

describe('KpSidenavContentDirective', () => {
  it('should create an instance', () => {
    const directive = new KpSidenavContentDirective();
    expect(directive).toBeTruthy();
  });
});
